package com.example.churc.messager;

/**
 * Created by churc on 6/30/2016.
 */
public class temp {
   // try {
        //SecureRandom random = new SecureRandom();
        //KeyGenerator aesGenerator = KeyGenerator.getInstance("AES","SC");
       // aesGenerator.init(128,random);
        //AESKey_test = aesGenerator.generateKey();       //Generate a test private key
       // RSAKeyGenParameterSpec spec = new RSAKeyGenParameterSpec(2048, RSAKeyGenParameterSpec.F4);
        //KeyPairGenerator generator = KeyPairGenerator.getInstance("RSA","SC");
        //generator.initialize(spec,random);
        //KeyPair_test = generator.generateKeyPair();
        //((TextView)findViewById(R.id.public_key_field)).setText(Base64.encodeToString(AESKey_test.getEncoded(),Base64.DEFAULT));

   // } //catch (NoSuchAlgorithmException e) {
        //e.printStackTrace();
    //} catch (NoSuchProviderException e) {
       // e.printStackTrace();
    //} catch (InvalidAlgorithmParameterException e) {
       // e.printStackTrace();
    //}

    //SharedPreferences sharedPref = context.getSharedPreferences(PUBLIC_KEY, Context.MODE_PRIVATE);
    //SharedPreferences.Editor editor = sharedPref.edit();
    //editor.putString(PUBLIC_KEY, Base64.encodeToString(AESKey_test.getEncoded(),Base64.DEFAULT));
    //editor.commit();
}
